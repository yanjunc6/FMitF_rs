// Online Gaming Platform — 80 transactions
// Tables: Player, Character, Item, Inventory, Guild, GuildMember,
//         Quest, QuestProgress, Achievement, PlayerAchievement,
//         Match, MatchPlayer, Rank, Currency, Market, MarketListing,
//         Friend, ChatMessage, Event, EventParticipant
// Breakdown: 10 read-only, 20 single-hop writes,
//            35 two-hop writes, 15 global scans

partition f(x: int) -> int { return x; }

table Player {
    primary id: int;
    node f(id);
    username: string;
    email: string;
    level: int;
    xp: int;
    status: int;
    created_at: int;
    invariant level >= 1;
}

table Character {
    primary id: int;
    node f(id);
    player_id: int;
    name: string;
    class_type: int;
    hp: int;
    max_hp: int;
    attack: int;
    defense: int;
    is_active: bool;
    invariant hp >= 0;
    invariant max_hp >= 1;
}

table Item {
    primary id: int;
    node f(id);
    name: string;
    item_type: int;
    rarity: int;
    base_value: int;
    is_tradeable: bool;
}

table Inventory {
    primary player_id: int;
    node f(player_id);
    item_id: int;
    quantity: int;
    slot: int;
    invariant quantity >= 0;
}

table Guild {
    primary id: int;
    node f(id);
    name: string;
    leader_id: int;
    member_count: int;
    max_members: int;
    guild_xp: int;
    treasury: int;
    rank: int;
    invariant member_count >= 0;
    invariant treasury >= 0;
}

table GuildMember {
    primary player_id: int;
    node f(player_id);
    guild_id: int;
    role: int;
    contribution: int;
    joined_at: int;
    invariant contribution >= 0;
}

table Quest {
    primary id: int;
    node f(id);
    name: string;
    min_level: int;
    xp_reward: int;
    currency_reward: int;
    max_completions: int;
    total_completions: int;
    invariant total_completions >= 0;
}

table QuestProgress {
    primary player_id: int;
    node f(player_id);
    quest_id: int;
    status: int;
    progress: int;
    goal: int;
    started_at: int;
    completed_at: int;
    invariant progress >= 0;
}

table Achievement {
    primary id: int;
    node f(id);
    name: string;
    description: string;
    points: int;
    unlock_count: int;
    invariant unlock_count >= 0;
}

table PlayerAchievement {
    primary player_id: int;
    node f(player_id);
    achievement_id: int;
    unlocked_at: int;
}

table Match {
    primary id: int;
    node f(id);
    mode: int;
    status: int;
    player_count: int;
    max_players: int;
    winner_id: int;
    started_at: int;
    finished_at: int;
    invariant player_count >= 0;
}

table MatchPlayer {
    primary match_id: int;
    node f(match_id);
    player_id: int;
    score: int;
    kills: int;
    deaths: int;
    assists: int;
    result: int;
    invariant score >= 0;
    invariant kills >= 0;
    invariant deaths >= 0;
}

table Rank {
    primary player_id: int;
    node f(player_id);
    season: int;
    rating: int;
    wins: int;
    losses: int;
    tier: int;
    invariant rating >= 0;
    invariant wins >= 0;
    invariant losses >= 0;
}

table Currency {
    primary player_id: int;
    node f(player_id);
    gold: int;
    gems: int;
    tokens: int;
    invariant gold >= 0;
    invariant gems >= 0;
    invariant tokens >= 0;
}

table Market {
    primary id: int;
    node f(id);
    name: string;
    listing_count: int;
    is_active: bool;
    invariant listing_count >= 0;
}

table MarketListing {
    primary id: int;
    node f(id);
    market_id: int;
    seller_id: int;
    item_id: int;
    quantity: int;
    price: int;
    status: int;
    created_at: int;
    invariant quantity >= 0;
    invariant price >= 1;
}

table Friend {
    primary player_id: int;
    node f(player_id);
    friend_id: int;
    status: int;
    created_at: int;
}

table ChatMessage {
    primary id: int;
    node f(id);
    sender_id: int;
    channel_id: int;
    content: string;
    sent_at: int;
}

table Event {
    primary id: int;
    node f(id);
    name: string;
    event_type: int;
    participant_count: int;
    max_participants: int;
    start_at: int;
    end_at: int;
    is_active: bool;
    invariant participant_count >= 0;
}

table EventParticipant {
    primary player_id: int;
    node f(player_id);
    event_id: int;
    score: int;
    rank: int;
    joined_at: int;
    invariant score >= 0;
}

// ── READ-ONLY (10) ─────────────────────────────────────────────────

transaction GetPlayer(playerId: int)
{
    hop {
        var p: Row<Player> = Player[id: playerId];
        to_unit(p.username);
        to_unit(p.level);
        to_unit(p.xp);
        to_unit(p.status);
    }
}

transaction GetCharacter(charId: int, playerId: int)
{
    hop {
        var c: Row<Character> = Character[id: charId];
        to_unit(c.name);
        to_unit(c.class_type);
        to_unit(c.hp);
        to_unit(c.max_hp);
        to_unit(c.attack);
        to_unit(c.defense);
    }
}

transaction GetCurrency(playerId: int)
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        to_unit(cur.gold);
        to_unit(cur.gems);
        to_unit(cur.tokens);
    }
}

transaction GetRank(playerId: int, season: int)
{
    hop {
        var r: Row<Rank> = Rank[player_id: playerId];
        to_unit(r.rating);
        to_unit(r.wins);
        to_unit(r.losses);
        to_unit(r.tier);
    }
}

transaction GetGuild(guildId: int)
{
    hop {
        var g: Row<Guild> = Guild[id: guildId];
        to_unit(g.name);
        to_unit(g.member_count);
        to_unit(g.guild_xp);
        to_unit(g.treasury);
        to_unit(g.rank);
    }
}

transaction GetMatch(matchId: int)
{
    hop {
        var m: Row<Match> = Match[id: matchId];
        to_unit(m.mode);
        to_unit(m.status);
        to_unit(m.player_count);
        to_unit(m.winner_id);
    }
}

transaction GetQuestProgress(playerId: int, questId: int)
{
    hop {
        var qp: Row<QuestProgress> = QuestProgress[player_id: playerId];
        to_unit(qp.status);
        to_unit(qp.progress);
        to_unit(qp.goal);
    }
}

transaction GetInventorySlot(playerId: int, slot: int)
{
    hop {
        var inv: Row<Inventory> = Inventory[player_id: playerId];
        to_unit(inv.item_id);
        to_unit(inv.quantity);
    }
}

transaction GetMarketListing(listingId: int, marketId: int)
{
    hop {
        var ml: Row<MarketListing> = MarketListing[id: listingId];
        to_unit(ml.seller_id);
        to_unit(ml.item_id);
        to_unit(ml.quantity);
        to_unit(ml.price);
        to_unit(ml.status);
    }
}

transaction GetEventInfo(eventId: int)
{
    hop {
        var ev: Row<Event> = Event[id: eventId];
        to_unit(ev.name);
        to_unit(ev.participant_count);
        to_unit(ev.max_participants);
        to_unit(ev.is_active);
    }
}

// ── SINGLE-HOP WRITES (20) ─────────────────────────────────────────

transaction UpdatePlayerStatus(playerId: int, newStatus: int)
{
    hop {
        Player[id: playerId].status = newStatus;
    }
}

transaction GainXP(playerId: int, amount: int)
    assume amount > 0;
{
    hop {
        var p: Row<Player> = Player[id: playerId];
        Player[id: playerId].xp = p.xp + amount;
    }
}

transaction HealCharacter(charId: int, playerId: int, healAmount: int)
    assume healAmount > 0;
{
    hop {
        var c: Row<Character> = Character[id: charId];
        var newHp: int = c.hp + healAmount;
        if newHp > c.max_hp { newHp = c.max_hp; }
        Character[id: charId].hp = newHp;
    }
}

transaction DamageCharacter(charId: int, playerId: int, dmg: int)
    assume dmg > 0;
{
    hop {
        var c: Row<Character> = Character[id: charId];
        if c.hp <= dmg { c.hp = 0; } else { c.hp = c.hp - dmg; }
    }
}

transaction AddGold(playerId: int, amount: int)
    assume amount > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].gold = cur.gold + amount;
    }
}

transaction AddGems(playerId: int, amount: int)
    assume amount > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].gems = cur.gems + amount;
    }
}

transaction SpendTokens(playerId: int, amount: int)
    assume amount > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        if cur.tokens < amount { return; }
        Currency[player_id: playerId].tokens = cur.tokens - amount;
    }
}

transaction RecordKill(matchId: int, playerId: int)
{
    hop {
        var mp: Row<MatchPlayer> = MatchPlayer[match_id: matchId];
        MatchPlayer[match_id: matchId].kills = mp.kills + 1;
        MatchPlayer[match_id: matchId].score = mp.score + 10;
    }
}

transaction RecordDeath(matchId: int, playerId: int)
{
    hop {
        var mp: Row<MatchPlayer> = MatchPlayer[match_id: matchId];
        MatchPlayer[match_id: matchId].deaths = mp.deaths + 1;
    }
}

transaction RecordAssist(matchId: int, playerId: int)
{
    hop {
        var mp: Row<MatchPlayer> = MatchPlayer[match_id: matchId];
        MatchPlayer[match_id: matchId].assists = mp.assists + 1;
        MatchPlayer[match_id: matchId].score = mp.score + 5;
    }
}

transaction UpdateRating(playerId: int, season: int, delta: int)
{
    hop {
        var r: Row<Rank> = Rank[player_id: playerId];
        var newRating: int = r.rating + delta;
        if newRating < 0 { newRating = 0; }
        Rank[player_id: playerId].rating = newRating;
    }
}

transaction IncrementWin(playerId: int, season: int)
{
    hop {
        var r: Row<Rank> = Rank[player_id: playerId];
        Rank[player_id: playerId].wins = r.wins + 1;
    }
}

transaction IncrementLoss(playerId: int, season: int)
{
    hop {
        var r: Row<Rank> = Rank[player_id: playerId];
        Rank[player_id: playerId].losses = r.losses + 1;
    }
}

transaction AddGuildXP(guildId: int, amount: int)
    assume amount > 0;
{
    hop {
        var g: Row<Guild> = Guild[id: guildId];
        Guild[id: guildId].guild_xp = g.guild_xp + amount;
    }
}

transaction DonateToGuildTreasury(guildId: int, amount: int)
    assume amount > 0;
{
    hop {
        var g: Row<Guild> = Guild[id: guildId];
        Guild[id: guildId].treasury = g.treasury + amount;
    }
}

transaction UpdateMemberContribution(guildId: int, playerId: int, amount: int)
    assume amount > 0;
{
    hop {
        var gm: Row<GuildMember> = GuildMember[player_id: playerId];
        GuildMember[player_id: playerId].contribution = gm.contribution + amount;
    }
}

transaction AdvanceQuestProgress(playerId: int, questId: int, amount: int)
    assume amount > 0;
{
    hop {
        var qp: Row<QuestProgress> = QuestProgress[player_id: playerId];
        QuestProgress[player_id: playerId].progress = qp.progress + amount;
    }
}

transaction CancelMarketListing(listingId: int, marketId: int)
{
    hop {
        var ml: Row<MarketListing> = MarketListing[id: listingId];
        if ml.status != 0 { return; }
        MarketListing[id: listingId].status = 2;
    }
}

transaction AcceptFriendRequest(playerId: int, friendId: int)
{
    hop {
        var f: Row<Friend> = Friend[player_id: playerId];
        if f.status != 0 { return; }
        Friend[player_id: playerId].status = 1;
    }
}

transaction SendChatMessage(senderId: int, channelId: int, msgId: int, content: string, sentAt: int)
{
    hop {
        ChatMessage[id: msgId].sender_id = senderId;
        ChatMessage[id: msgId].channel_id = channelId;
        ChatMessage[id: msgId].content = content;
        ChatMessage[id: msgId].sent_at = sentAt;
    }
}

// ── TWO-HOP WRITES (35) ─────────────────────────────────────────────

transaction SpendGold(playerId: int, amount: int)
    assume amount > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        if cur.gold < amount { return; }
        Currency[player_id: playerId].gold = cur.gold - amount;
    }
    hop {
        var p: Row<Player> = Player[id: playerId];
        Player[id: playerId].xp = p.xp + 1;
    }
}

transaction LevelUp(playerId: int, charId: int, xpThreshold: int)
    assume xpThreshold > 0;
{
    hop {
        var p: Row<Player> = Player[id: playerId];
        if p.xp < xpThreshold { return; }
        Player[id: playerId].xp = p.xp - xpThreshold;
        Player[id: playerId].level = p.level + 1;
    }
    hop {
        var c: Row<Character> = Character[id: charId];
        Character[id: charId].max_hp = c.max_hp + 10;
        Character[id: charId].hp = c.hp + 10;
        Character[id: charId].attack = c.attack + 2;
        Character[id: charId].defense = c.defense + 1;
    }
}

transaction CompleteQuest(playerId: int, questId: int, xpReward: int, goldReward: int)
    assume xpReward > 0;
    assume goldReward >= 0;
{
    hop {
        var qp: Row<QuestProgress> = QuestProgress[player_id: playerId];
        if qp.status != 0 { return; }
        if qp.progress < qp.goal { return; }
        QuestProgress[player_id: playerId].status = 1;
    }
    hop {
        var p: Row<Player> = Player[id: playerId];
        Player[id: playerId].xp = p.xp + xpReward;
        var cur: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].gold = cur.gold + goldReward;
    }
}

transaction UnlockAchievement(playerId: int, achievementId: int, unlockTime: int)
{
    hop {
        var ach: Row<Achievement> = Achievement[id: achievementId];
        Achievement[id: achievementId].unlock_count = ach.unlock_count + 1;
    }
    hop {
        PlayerAchievement[player_id: playerId].achievement_id = achievementId;
        PlayerAchievement[player_id: playerId].unlocked_at = unlockTime;
    }
}

transaction JoinMatch(matchId: int, playerId: int)
{
    hop {
        var m: Row<Match> = Match[id: matchId];
        if m.status != 0 { return; }
        if m.player_count >= m.max_players { return; }
        Match[id: matchId].player_count = m.player_count + 1;
    }
    hop {
        var p: Row<Player> = Player[id: playerId];
        if p.status != 1 { return; }
        Player[id: playerId].status = 2;
    }
}

transaction LeaveMatch(matchId: int, playerId: int)
{
    hop {
        var m: Row<Match> = Match[id: matchId];
        if m.player_count <= 0 { return; }
        Match[id: matchId].player_count = m.player_count - 1;
    }
    hop {
        Player[id: playerId].status = 1;
    }
}

transaction FinishMatch(matchId: int, winnerId: int)
{
    hop {
        var m: Row<Match> = Match[id: matchId];
        if m.status != 1 { return; }
        Match[id: matchId].status = 2;
        Match[id: matchId].winner_id = winnerId;
    }
    hop {
        MatchPlayer[match_id: matchId].result = 1;
    }
}

transaction StartMatch(matchId: int)
{
    hop {
        var m: Row<Match> = Match[id: matchId];
        if m.status != 0 { return; }
        if m.player_count < 2 { return; }
        Match[id: matchId].status = 1;
    }
    hop {
        var m2: Row<Match> = Match[id: matchId];
        to_unit(m2.started_at);
    }
}

transaction BuyItemFromMarket(buyerId: int, listingId: int, marketId: int)
{
    hop {
        var ml: Row<MarketListing> = MarketListing[id: listingId];
        if ml.status != 0 { return; }
        MarketListing[id: listingId].status = 1;
    }
    hop {
        var cur: Row<Currency> = Currency[player_id: buyerId];
        Currency[player_id: buyerId].gold = cur.gold - 1;
        var m: Row<Market> = Market[id: marketId];
        if m.listing_count > 0 {
            Market[id: marketId].listing_count = m.listing_count - 1;
        }
    }
}

transaction ListItemOnMarket(sellerId: int, marketId: int, listingId: int, itemId: int, qty: int, price: int)
    assume qty > 0;
    assume price > 0;
{
    hop {
        var inv: Row<Inventory> = Inventory[player_id: sellerId];
        if inv.quantity < qty { return; }
        Inventory[player_id: sellerId].quantity = inv.quantity - qty;
    }
    hop {
        var m: Row<Market> = Market[id: marketId];
        Market[id: marketId].listing_count = m.listing_count + 1;
        MarketListing[id: listingId].market_id = marketId;
        MarketListing[id: listingId].seller_id = sellerId;
        MarketListing[id: listingId].item_id = itemId;
        MarketListing[id: listingId].quantity = qty;
        MarketListing[id: listingId].price = price;
        MarketListing[id: listingId].status = 0;
    }
}

transaction TransferGoldToGuild(playerId: int, guildId: int, amount: int)
    assume amount > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        if cur.gold < amount { return; }
        Currency[player_id: playerId].gold = cur.gold - amount;
    }
    hop {
        var g: Row<Guild> = Guild[id: guildId];
        Guild[id: guildId].treasury = g.treasury + amount;
    }
}

transaction WithdrawFromGuildTreasury(guildId: int, playerId: int, amount: int)
    assume amount > 0;
{
    hop {
        var g: Row<Guild> = Guild[id: guildId];
        if g.treasury < amount { return; }
        Guild[id: guildId].treasury = g.treasury - amount;
    }
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].gold = cur.gold + amount;
    }
}

transaction JoinGuild(playerId: int, guildId: int, joinTime: int)
{
    hop {
        var g: Row<Guild> = Guild[id: guildId];
        if g.member_count >= g.max_members { return; }
        Guild[id: guildId].member_count = g.member_count + 1;
    }
    hop {
        GuildMember[player_id: playerId].guild_id = guildId;
        GuildMember[player_id: playerId].player_id = playerId;
        GuildMember[player_id: playerId].role = 0;
        GuildMember[player_id: playerId].contribution = 0;
        GuildMember[player_id: playerId].joined_at = joinTime;
    }
}

transaction LeaveGuild(playerId: int, guildId: int)
{
    hop {
        var g: Row<Guild> = Guild[id: guildId];
        if g.member_count <= 0 { return; }
        Guild[id: guildId].member_count = g.member_count - 1;
    }
    hop {
        var p: Row<Player> = Player[id: playerId];
        to_unit(p.status);
    }
}

transaction PromoteGuildMember(guildId: int, playerId: int)
{
    hop {
        var gm: Row<GuildMember> = GuildMember[player_id: playerId];
        if gm.role >= 2 { return; }
        GuildMember[player_id: playerId].role = gm.role + 1;
    }
    hop {
        var g: Row<Guild> = Guild[id: guildId];
        to_unit(g.name);
    }
}

transaction JoinEvent(eventId: int, playerId: int, joinTime: int)
{
    hop {
        var ev: Row<Event> = Event[id: eventId];
        if !ev.is_active { return; }
        if ev.participant_count >= ev.max_participants { return; }
        Event[id: eventId].participant_count = ev.participant_count + 1;
    }
    hop {
        EventParticipant[player_id: playerId].event_id = eventId;
        EventParticipant[player_id: playerId].player_id = playerId;
        EventParticipant[player_id: playerId].score = 0;
        EventParticipant[player_id: playerId].joined_at = joinTime;
    }
}

transaction SubmitEventScore(eventId: int, playerId: int, score: int)
    assume score >= 0;
{
    hop {
        var ep: Row<EventParticipant> = EventParticipant[player_id: playerId];
        if ep.score >= score { return; }
        EventParticipant[player_id: playerId].score = score;
    }
    hop {
        var ev: Row<Event> = Event[id: eventId];
        to_unit(ev.name);
    }
}

transaction EquipItem(playerId: int, charId: int, itemId: int)
{
    hop {
        var inv: Row<Inventory> = Inventory[player_id: playerId];
        if inv.item_id != itemId { return; }
        if inv.quantity <= 0 { return; }
        Inventory[player_id: playerId].quantity = inv.quantity - 1;
    }
    hop {
        var c: Row<Character> = Character[id: charId];
        Character[id: charId].attack = c.attack + 5;
        Character[id: charId].defense = c.defense + 3;
    }
}

transaction CraftItem(playerId: int, materialId: int, craftedItemId: int, cost: int)
    assume cost > 0;
{
    hop {
        var inv: Row<Inventory> = Inventory[player_id: playerId];
        if inv.item_id != materialId { return; }
        if inv.quantity < cost { return; }
        Inventory[player_id: playerId].quantity = inv.quantity - cost;
    }
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].gold = cur.gold + 1;
    }
}

transaction ExchangeGemsForGold(playerId: int, gems: int, goldRate: int)
    assume gems > 0;
    assume goldRate > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        if cur.gems < gems { return; }
        Currency[player_id: playerId].gems = cur.gems - gems;
    }
    hop {
        var cur2: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].gold = cur2.gold + gems * goldRate;
    }
}

transaction TransferItem(senderId: int, receiverId: int, itemId: int, qty: int)
    assume qty > 0;
{
    hop {
        var inv: Row<Inventory> = Inventory[player_id: senderId];
        if inv.item_id != itemId { return; }
        if inv.quantity < qty { return; }
        Inventory[player_id: senderId].quantity = inv.quantity - qty;
    }
    hop {
        var inv2: Row<Inventory> = Inventory[player_id: receiverId];
        Inventory[player_id: receiverId].quantity = inv2.quantity + qty;
    }
}

transaction PostMatchReward(matchId: int, playerId: int, xp: int, gold: int)
    assume xp >= 0;
    assume gold >= 0;
{
    hop {
        var mp: Row<MatchPlayer> = MatchPlayer[match_id: matchId];
        to_unit(mp.result);
    }
    hop {
        var p: Row<Player> = Player[id: playerId];
        Player[id: playerId].xp = p.xp + xp;
        Player[id: playerId].status = 1;
        var cur: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].gold = cur.gold + gold;
    }
}

transaction RenameCharacter(playerId: int, charId: int, newName: string, gemCost: int)
    assume gemCost > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        if cur.gems < gemCost { return; }
        Currency[player_id: playerId].gems = cur.gems - gemCost;
    }
    hop {
        Character[id: charId].name = newName;
    }
}

transaction SetActiveCharacter(playerId: int, charId: int)
{
    hop {
        var p: Row<Player> = Player[id: playerId];
        to_unit(p.level);
    }
    hop {
        Character[id: charId].is_active = true;
    }
}

transaction SellItemToShop(playerId: int, itemId: int, qty: int, goldValue: int)
    assume qty > 0;
    assume goldValue > 0;
{
    hop {
        var inv: Row<Inventory> = Inventory[player_id: playerId];
        if inv.item_id != itemId { return; }
        if inv.quantity < qty { return; }
        Inventory[player_id: playerId].quantity = inv.quantity - qty;
    }
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].gold = cur.gold + goldValue;
    }
}

transaction BlockPlayer(playerId: int, targetId: int)
{
    hop {
        Friend[player_id: playerId].friend_id = targetId;
        Friend[player_id: playerId].status = 2;
    }
    hop {
        var p: Row<Player> = Player[id: targetId];
        to_unit(p.username);
    }
}

transaction UnlockQuestReward(playerId: int, questId: int, tokens: int)
    assume tokens >= 0;
{
    hop {
        var q: Row<Quest> = Quest[id: questId];
        Quest[id: questId].total_completions = q.total_completions + 1;
    }
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].tokens = cur.tokens + tokens;
    }
}

transaction ResetCharacterHP(charId: int, playerId: int)
{
    hop {
        var c: Row<Character> = Character[id: charId];
        Character[id: charId].hp = c.max_hp;
    }
    hop {
        var p: Row<Player> = Player[id: playerId];
        to_unit(p.level);
    }
}

transaction UpgradeCharacterStat(charId: int, playerId: int, gemCost: int)
    assume gemCost > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        if cur.gems < gemCost { return; }
        Currency[player_id: playerId].gems = cur.gems - gemCost;
    }
    hop {
        var c: Row<Character> = Character[id: charId];
        Character[id: charId].attack = c.attack + 3;
        Character[id: charId].defense = c.defense + 2;
        Character[id: charId].max_hp = c.max_hp + 20;
    }
}

transaction BuyTokenBundle(playerId: int, goldCost: int, tokens: int)
    assume goldCost > 0;
    assume tokens > 0;
{
    hop {
        var cur: Row<Currency> = Currency[player_id: playerId];
        if cur.gold < goldCost { return; }
        Currency[player_id: playerId].gold = cur.gold - goldCost;
    }
    hop {
        var cur2: Row<Currency> = Currency[player_id: playerId];
        Currency[player_id: playerId].tokens = cur2.tokens + tokens;
    }
}

// ── GLOBAL SCANS (15) ────────────────────────────────────────────────

transaction ScanOnlinePlayers(n: int)
{
    @global hop {
        for (var it: Iterator<Player> = scan(Player, n, __slice__); hasNext(it); it = next(it)) {
            var pid = get_id(it);
            var p: Row<Player> = Player[id: pid];
            to_unit(p.username);
            to_unit(p.status);
        }
    }
}

transaction ScanActiveMatches(n: int)
{
    @global hop {
        for (var it: Iterator<Match> = scan(Match, n, __slice__); hasNext(it); it = next(it)) {
            var mid = get_id(it);
            var m: Row<Match> = Match[id: mid];
            if m.status == 1 {
                to_unit(m.player_count);
            }
        }
    }
}

transaction ScanActiveEvents(n: int)
{
    @global hop {
        for (var it: Iterator<Event> = scan(Event, n, __slice__); hasNext(it); it = next(it)) {
            var eid = get_id(it);
            var ev: Row<Event> = Event[id: eid];
            if ev.is_active {
                to_unit(ev.name);
                to_unit(ev.participant_count);
            }
        }
    }
}

transaction ScanTopRankedPlayers(n: int)
{
    @global hop {
        for (var it: Iterator<Rank> = scan(Rank, n, __slice__); hasNext(it); it = next(it)) {
            var pid = get_player_id(it);
            var r: Row<Rank> = Rank[player_id: pid];
            to_unit(r.rating);
            to_unit(r.tier);
        }
    }
}

transaction ScanGuildLeaderboard(n: int)
{
    @global hop {
        for (var it: Iterator<Guild> = scan(Guild, n, __slice__); hasNext(it); it = next(it)) {
            var gid = get_id(it);
            var g: Row<Guild> = Guild[id: gid];
            to_unit(g.name);
            to_unit(g.guild_xp);
            to_unit(g.rank);
        }
    }
}

transaction ScanOpenMarketListings(n: int)
{
    @global hop {
        for (var it: Iterator<MarketListing> = scan(MarketListing, n, __slice__); hasNext(it); it = next(it)) {
            var lid = get_id(it);
            var ml: Row<MarketListing> = MarketListing[id: lid];
            if ml.status == 0 {
                to_unit(ml.item_id);
                to_unit(ml.price);
                to_unit(ml.quantity);
            }
        }
    }
}

transaction ScanAllQuests(n: int)
{
    @global hop {
        for (var it: Iterator<Quest> = scan(Quest, n, __slice__); hasNext(it); it = next(it)) {
            var qid = get_id(it);
            var q: Row<Quest> = Quest[id: qid];
            to_unit(q.name);
            to_unit(q.xp_reward);
            to_unit(q.currency_reward);
        }
    }
}

transaction ScanAllAchievements(n: int)
{
    @global hop {
        for (var it: Iterator<Achievement> = scan(Achievement, n, __slice__); hasNext(it); it = next(it)) {
            var aid = get_id(it);
            var a: Row<Achievement> = Achievement[id: aid];
            to_unit(a.name);
            to_unit(a.points);
            to_unit(a.unlock_count);
        }
    }
}

transaction ScanAllItems(n: int)
{
    @global hop {
        for (var it: Iterator<Item> = scan(Item, n, __slice__); hasNext(it); it = next(it)) {
            var iid = get_id(it);
            var item: Row<Item> = Item[id: iid];
            to_unit(item.name);
            to_unit(item.rarity);
            to_unit(item.base_value);
        }
    }
}

transaction ScanWaitingMatches(n: int)
{
    @global hop {
        for (var it: Iterator<Match> = scan(Match, n, __slice__); hasNext(it); it = next(it)) {
            var mid = get_id(it);
            var m: Row<Match> = Match[id: mid];
            if m.status == 0 {
                to_unit(m.mode);
                to_unit(m.player_count);
                to_unit(m.max_players);
            }
        }
    }
}

transaction ScanHighValueListings(n: int, minPrice: int)
    assume minPrice > 0;
{
    @global hop {
        for (var it: Iterator<MarketListing> = scan(MarketListing, n, __slice__); hasNext(it); it = next(it)) {
            var lid = get_id(it);
            var ml: Row<MarketListing> = MarketListing[id: lid];
            if ml.status == 0 {
                if ml.price >= minPrice {
                    to_unit(ml.seller_id);
                    to_unit(ml.price);
                }
            }
        }
    }
}

transaction ScanRichPlayers(n: int)
{
    @global hop {
        for (var it: Iterator<Currency> = scan(Currency, n, __slice__); hasNext(it); it = next(it)) {
            var pid = get_player_id(it);
            var cur: Row<Currency> = Currency[player_id: pid];
            to_unit(cur.gold);
            to_unit(cur.gems);
        }
    }
}

transaction ScanLargeGuilds(n: int)
{
    @global hop {
        for (var it: Iterator<Guild> = scan(Guild, n, __slice__); hasNext(it); it = next(it)) {
            var gid = get_id(it);
            var g: Row<Guild> = Guild[id: gid];
            to_unit(g.name);
            to_unit(g.member_count);
            to_unit(g.treasury);
        }
    }
}

transaction ScanEventScoreboard(eventId: int, n: int)
{
    @global hop {
        for (var it: Iterator<EventParticipant> = scan(EventParticipant, n, __slice__); hasNext(it); it = next(it)) {
            var pid = get_player_id(it);
            var ep: Row<EventParticipant> = EventParticipant[player_id: pid];
            if ep.event_id == eventId {
                to_unit(ep.score);
                to_unit(ep.rank);
            }
        }
    }
}

transaction ScanRecentChatMessages(channelId: int, n: int)
{
    @global hop {
        for (var it: Iterator<ChatMessage> = scan(ChatMessage, n, __slice__); hasNext(it); it = next(it)) {
            var mid = get_id(it);
            var msg: Row<ChatMessage> = ChatMessage[id: mid];
            if msg.channel_id == channelId {
                to_unit(msg.sender_id);
                to_unit(msg.content);
                to_unit(msg.sent_at);
            }
        }
    }
}
