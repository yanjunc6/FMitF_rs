// Expectation: canonical loop with known trip count (3) should be fully unrolled.

function known_trip_simple() -> int {
    var sum: int = 0;
    for (var i: int = 0; i < 3; i = i + 1) {
        sum = sum + i;
    }
    return sum;
}
