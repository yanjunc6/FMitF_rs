// Expectation: canonical decrement loop with known trip count should be unrolled.

function known_trip_negative_step() -> int {
    var sum: int = 0;
    for (var i: int = 5; i > 0; i = i - 1) {
        sum = sum + i;
    }
    return sum;
}
