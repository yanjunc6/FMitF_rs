// Expectation: nested canonical loops with known counts should both unroll.

function nested_known_loops() -> int {
    var sum: int = 0;
    for (var i: int = 0; i < 2; i = i + 1) {
        for (var j: int = 0; j < 3; j = j + 1) {
            sum = sum + i + j;
        }
    }
    return sum;
}
